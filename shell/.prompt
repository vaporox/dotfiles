#!/bin/bash

first='true'

PROMPT_COMMAND='newline'

PS1='\[\e[36m\]\w\[\e[0m\]\n> '

clear() {
	first='true'
	command clear
}

newline() {
	if [[ "$first" == 'true' ]]; then
		first='false'
	else
		echo
	fi
}
